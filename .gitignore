# Rust build output
/target

# Whisper models (large binary files, user-provided)
models/*.bin
snap/models/*.bin

# Distribution packages
*.deb
*.tar.gz

# Distribution staging directory (generated by dist.sh / build.sh)
/dist/

# Packaging staging directory (generated by package-deb.sh)
packaging/meet-whisperer_*/

# Snap build artifacts
*.snap
snap/.snapcraft/
snap/parts/
snap/stage/
snap/prime/
snap/bin/scrivano

# Local IDE / agent tool settings (machine-specific paths)
.claude/settings.local.json

# Local debug artifacts
scrivano-debug.log

# Temporary root-level GUI asset staging (Snap listing prep)
/gui/


*.prompt.md
.cargo/
